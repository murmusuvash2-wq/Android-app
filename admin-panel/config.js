// admin-panel/config.js
// Optional runtime environment config injected during Vercel build.
// If empty, the admin panel uses the on-screen configuration modal.
window.__ENV__ = window.__ENV__ || {};
