package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Final Approved Color System Tokens
val BackgroundColor = Color(0xFFFAF9F6)     // Main background of the entire light-theme application
val SurfaceColor = Color(0xFFFFFFFF)        // Cards, bottom sheets, dialogs, elevated surfaces
val SurfaceVariantColor = Color(0xFFF3F1ED) // Secondary containers, subtle inactive surfaces, chips
val DeepForest = Color(0xFF3F5147)          // Primary Accent: active tabs, selected states, interactive links
val PrimaryText = Color(0xFF1E1E24)         // Screen titles, headings, product names, important text
val SecondaryText = Color(0xFF4B5563)       // Descriptions, supporting text, metadata, secondary labels
val TertiaryText = Color(0xFF6B7280)        // Tertiary / Muted Text
val DisabledColor = Color(0xFF9CA3AF)       // Disabled states, placeholders, low-priority metadata
val ChampagneGold = Color(0xFFC9A961)       // VERY SPARINGLY: rating stars, credit highlights, premium highlights
val BorderColor = Color(0xFFE5E1DC)         // Subtle borders, dividers and outlines
val PurchaseCTA = Color(0xFF1E1E24)         // Primary purchase buttons (Buy ↗)
val CTAText = Color(0xFFFFFFFF)             // CTA Text

// Centralized Semantic Aliases
val WarmIvory = BackgroundColor
val Charcoal = PrimaryText
val SoftCharcoal = SecondaryText
val OffWhite = SurfaceColor
val WarmGray = BorderColor
val White = SurfaceColor

